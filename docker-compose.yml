version: '3.8'

services:
  seo-system:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - NODE_ENV=production
